services:
  - type: web
    name: family-bracket-2026
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server.py
    envVars:
      - key: PORT
        value: "10000"
      - key: DB_PATH
        value: /opt/render/project/src/family_bracket.db
